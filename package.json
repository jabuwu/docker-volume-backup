{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "npx lerna run dev --stream",
    "gen": "npx lerna run gen --stream --scope docker-volume-backup-ui"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  }
}
