fragment storageData on Storage {
  name
  type
  localFileSystem {
    name
    path
    prefix
  }
  s3Bucket {
    name
    bucket
    prefix
  }
  ftpServer {
    name
    host
    port
    user
    secure
    prefix
  }
}