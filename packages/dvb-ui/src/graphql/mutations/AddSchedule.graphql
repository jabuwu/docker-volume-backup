#import "../fragments/ScheduleData"
mutation AddSchedule($volume: String!, $storage: String!, $hours: Int!, $stopContainers: Boolean!) {
  addSchedule(volume: $volume, storage: $storage, hours: $hours, stopContainers: $stopContainers) {
    ...scheduleData
  }
}